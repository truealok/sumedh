{
  "name": "riceguard-backend",
  "private": true,
  "description": "Wrapper scripts for the Python FastAPI backend. The real dependencies live in requirements.txt.",
  "scripts": {
    "dev": "python -m uvicorn main:app --reload --port 8000",
    "start": "python -m uvicorn main:app --port 8000",
    "install-deps": "python -m pip install -r requirements.txt"
  }
}
